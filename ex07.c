#include <stdio.h>
// exercicio 7 verificar se os numeros inseridos atendem os creiterios para a soma
int main(void) {
  float a, b, c, d;
  
  printf("Digite o valor de a: ");
  scanf("%d",&a);
  printf("Digite o valor de b: ");
  scanf("%d",&b);
  printf("Digite o valor de c: ");
  scanf("%d",&c);
  printf("Digite o valor de d: ");
  scanf("%d",&d);
  if(a>b && c>d){;
    if(c+d <a +b);
      printf("Criterios aceitos");
      }else{
      printf("Erro");
      }
  return 0;
}